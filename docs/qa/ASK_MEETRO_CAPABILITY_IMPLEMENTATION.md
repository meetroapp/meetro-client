# Ask Meetro capability implementation — candidate report

Status: implementation and automated gates complete through H. STOPPED at I (physical iPhone/iPad core QA). J (new iPhone non-Home header access) has deliberately not started; it requires physical core PASS. These candidates are uncommitted, unpushed and undeployed.

1. **Client starting authority:** `/private/tmp/meetro-home-dashboard-ask-meetro`, HEAD `329fd5001509e8cff8af726f53b035e55c118a97`, clean at task start. That commit already contains the previous nine-file intent/voice repair (+655/−199) above certified `50112adf2e8e80c5df7660f513a143e67a78a48e` / build 904563. The supplied description of the repair as uncommitted was stale. Nothing was discarded or recommitted. Client remote staging was independently verified as `11d1a62bf1ca796b60e232fcbc32b40d96b9a9b9`.
2. **Server remote-staging authority:** `574f3f99e9290ca4f959ffc26e13d771e503400d`, verified by remote lookup on 2026-09-09. A new detached implementation worktree was created at `/private/tmp/meetro-ask-capability-server-574f3f9` using the isolated bare clone; no protected checkout was fetched into or modified. The remote branch SHA is not proof of the currently deployed Railway revision.
3. **Existing Companion contract:** authenticated `/api/companion/ask`; exact operation/capability registry; server context builders, engine registry and orchestrator; actor-scoped durable idempotency; optional usage finalizer. Six existing operations remain `job_request.interpret`, `quote.compose`, `quick_quote.photo_assist`, `evaluation.assist`, `estimate.compose`, `invoice.assist`. No general conversation existed. Private Quick Quote session continuation is a separate governed path and was not repurposed.
4. **Conversational operation contract:** new server-owned `companion.converse`, capability of the same name. Maximum question 5,000 characters; optional eight-turn history, at most 2,000 characters/turn and 8,000 total; optional single exact record pointer. Output is bounded text with server-added `CONVERSATIONAL_NON_CANONICAL` and `directMutationAllowed:false`. Additional provider action/route/patch properties are rejected. Full schema/read coverage is documented in the server candidate's `docs/qa/ASK_MEETRO_CONVERSATION_CONTRACT.md`.
5. **Provider/orchestrator path:** existing canonical registry → operation context builder → existing orchestrator → configured `workflow_assistance` adapter → OpenAI Responses → strict text-result parser. Existing server configuration/credentials remain unchanged. `store:false` and an operation-specific 2,500 output-token ceiling apply; no client provider SDK/key or retired endpoint.
6. **When an AI call occurs:** ordinary questions, explanation, troubleshooting, comparison, summary, interpretation, drafting and mixed-message explanation use the new conversational operation after server validation/authorization. Provider execution is controlled by the registered operation, not by arbitrary client provider/model fields.
7. **When no AI call occurs:** known Communication/Home/Leads navigation; viewing exact Quote/Invoice/Job/Conversation routes; showing an exact Job schedule; exact professional Job status via its existing GET; deterministic missing-context/role clarification; existing governed proposal planning/Quote lookup/review. No automatic provider call on mount, keystrokes, attachment selection, voice transcription or Review.
8. **Informational intent:** clause openings identify questions/help; embedded what/how/why/whether/explain inside a clear operational clause do not suppress that command. Informational replies render as text with zero Review cards.
9. **Governed action behavior:** existing resolver handles exact Job, Quote, Invoice, payment/deposit, schedule and completion proposals. Reporting payment/approval/completion is not a command to record it. Unsupported or unidentified operations do not become placeholder Review actions.
10. **Mixed intent:** the informational portion may receive a provider response; the client returns zero proposals and appends an explicit notice that the change has not been proposed and must be sent separately for exact-record Review. Includes implicit-subject wording such as “Explain this invoice and then update the due date.”
11. **Exact-record authority:** route pointers are context only. Server reads independently enforce existing owner/participant/capability rules. No display label, browser cache or conversation claim becomes canonical evidence. Unsupported/malformed/unauthorized pointers fail closed.
12. **Review → Confirm & Apply:** unchanged. Existing canonical completion adapter still requires exact review/current version and explicit confirmation before POST. Other actions continue through their existing record review workspaces. Provider text is never fed into the action planner, route setter or apply handler.
13. **Context handling:** one exact Job, saved document draft, Quote, Invoice, Evaluation, Visit, Customer Relationship, Conversation or owned Job Request. Existing authorized readers are reused; owned Job Request uses the same exact ID/owner predicate as its existing GET route. Descriptive fields only, bounded excerpts; no email/phone/media URLs/internal costs/permissions. Homeowner Job context uses the customer-owned Job/issued-Quote projection rather than private live-work state. Conversation excerpt is bounded to its first 12 messages and is not represented as complete history. Business Contact-only context without an exact supported relationship is not fabricated.
14. **General help:** wired to the real provider-backed server adapter, with practical safety/uncertainty instructions. Tests intercept provider transport; useful live response quality remains unverified until an approved running candidate is available. There is no canned-answer substitute.
15. **Quote capability:** authorized explanation/scope drafting through conversational text; existing governed composition and exact Quote → Invoice navigation remain intact. Approval is not completion; no numbering, save or issue change.
16. **Invoice capability:** authorized explanation and wording assistance; existing Invoice assistance/working-document review remain intact. No inferred payment, totals mutation, numbering or silent save.
17. **Job capability:** authorized state explanation, exact deterministic status read, existing schedule/work/completion reviews. Canonical Job completion remains a separately confirmed command.
18. **Customer/Communication capability:** bounded professional-owned relationship/activity and participant-authorized conversation excerpts support summaries and reply drafting; no underlying customer/conversation write. Exact supported context is required for record-specific claims.
19. **Voice mechanism:** existing Capacitor plugin → Apple Speech/AVFoundation; existing browser SpeechRecognition fallback. No dependency or permission-description change.
20. **Voice permission/lifecycle:** retained capability/grant/denial/retry/start/stop/partial/final/error handling. Added distinct timeout result/notice and native background observer; visibility/unmount cancellation retires late callbacks. Transcription stays editable and never auto-sends/applies. Native permission/microphone success still requires device QA.
21. **Attach/Photo:** controls and local selection remain, including the existing Friends & Family upload deferral. The local filename display was repaired. Selected files block this text-only submission with an explicit not-sent message; no hidden upload or claim of analysis. Canonical evidence must use its existing governed transport/review/save path.
22. **Session continuity:** existing single workspace/host session retained. Provider receives only bounded user/assistant discussion history. Account/auth changes still clear session; workspace teardown aborts pending conversation delivery and prevents a late provider handoff. Uncertain transport retries retain the idempotency key; confirmed failed ledger operations allow a fresh key on explicit retry.
23. **Role boundaries:** supported authenticated homeowner/professional roles; server owner/participant/capability checks remain decisive. Professional private drafts/evaluations/customer relationships are not exposed to homeowners. No team-permission changes.
24. **Failure behavior:** A 45-second client transport timeout aborts a stalled request while retaining the key for an uncertain-outcome retry. provider/transport/parser/lookup/context failures preserve typed input, remove stale proposals and allow retry without fake success/action cards. Provider output with action-authority fields is rejected. Plain action-looking text remains inert text.
25. **iPhone access model:** existing Home hero entry preserved. New labeled non-Home header control is NOT implemented yet because required physical core gate I has not passed. No sixth tab or new floating orb was added.
26. **iPad/web access model:** existing persistent shell entry and navigation unchanged. One canonical Ask workspace; no parallel per-page AI chat system.
27. **Exact client files changed:** `ios/App/App/SpeechRecognition.swift`; `src/components/AskMeetroWorkspace.jsx`; `src/utils/askMeetro.js`; `src/utils/askMeetroVoiceInput.js`; `tests/askMeetroVoiceInput.test.js`; `tests/askMeetroWorkspace.test.js`; new `src/utils/askMeetroConversation.js`; new `tests/askMeetroConversation.test.js`; this new report `docs/qa/ASK_MEETRO_CAPABILITY_IMPLEMENTATION.md`.
28. **Exact server files changed:** `server/intelligence/intelligenceOperationRegistry.js`; `server/intelligence/openAiWorkflowProvider.js`; `test/intelligenceGatewayRuntime.test.js`; `test/jobRequestInterpretOperation.test.js`; new `server/intelligence/conversationContext.js`; new `server/intelligence/operations/companionConverse.js`; new `test/companionConversation.test.js`; new `docs/qa/ASK_MEETRO_CONVERSATION_CONTRACT.md`.
29. **Exact untracked files:** the three client and four server files explicitly marked “new” above. Dependencies were copied into the isolated server worktree from existing installed dependencies; node_modules is ignored. No package/lockfile changes.
30. **Client focused tests:** 145 passed across Ask authority, workspace, conversation and voice suites; zero failures. Voice suite contains 19 tests. Current continuation adds 44 tests over starting candidate.
31. **Client full suite:** 4,460 passed; zero failures/skips.
32. **Server focused tests:** 90 passed across conversation, gateway runtime/routes, operation idempotency and provider suites; zero failures. Includes 46 new conversation tests.
33. **Server full suite:** 2,362 discovered; 2,293 passed, 69 database-dependent tests skipped, zero failures. Database/provider environment variables were removed for isolation. Initial run hit four sandbox local-listener restrictions and one expected registry-list update; final run passed after resolving both. No test database or production database was contacted.
34. **ESLint/diff checks:** changed client JS/JSX has zero errors, zero warnings and zero added diagnostics against starting HEAD. Both repositories pass `git diff --check`. Server has no configured ESLint script. No blanket lint disables.
35. **Client staging build:** passed with supplied staging URL/environment; existing large-chunk advisory remains. No staging push/deployment.
36. **Native build:** unsigned iOS Simulator build passed using an isolated native copy and cached dependencies. Initial sandbox attempt could not write Xcode caches; authorized cache access resolved it. No signing, device installation or build-number change.
37. **Physical-device certification:** NOT certified. Browser fixture checks at 375/390/393/428/1024/1366 passed: informational response visible, zero informational actions, all four Review controls at least 84px wide and single-line, no horizontal document overflow. These are fixture-only presentation checks, not live AI or physical microphone proof.
38. **Unresolved gate:** I requires physical iPhone/iPad testing of real typed response, contextual explanation, first permission, speech, editable transcript, manual Send, stop/retry, denial/recovery, interruption/background and Review authority. The new server operation is not deployed, so current remote staging cannot deliver it. Stop before push/deployment as instructed; arrange an explicitly approved running candidate and device QA before J/K. No claim of full production certification.
39. **Production untouched:** no production/staging deployment, provider secret change, real provider request, database/schema change, protected checkout edit, commit or push. Candidate HEADs remain the starting SHAs.
40. **Lifecycle/numbering/authority:** no lifecycle handler, numbering allocator, save/discard, payment evidence, customer relationship permission, schedule gate, approval or completion authority changed. Existing six Companion operations and canonical workflow tests remain green. The new operation only reads authorized records and writes the existing Intelligence idempotency/result ledger when invoked.
